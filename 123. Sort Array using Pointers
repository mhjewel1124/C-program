#include <stdio.h>
int main(){
int n; scanf("%d", &n);
int a[n];
for(int i=0;i<n;i++) scanf("%d", &a[i]);
int *p = a;
for(int i=0;i<n;i++)
for(int j=i+1;j<n;j++)
if(*(p+j) < *(p+i)){
int t = *(p+i);
*(p+i) = *(p+j);
*(p+j) = t;
}
for(int i=0;i<n;i++) printf("%d ", *(p+i));
return 0;
}
