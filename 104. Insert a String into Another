#include <stdio.h>
#include <string.h>

int main(){
    char a[200], b[100];
    int pos;
    gets(a);
    gets(b);
    scanf("%d",&pos);

    char temp[200];
    strcpy(temp, a+pos);

    a[pos] = '\0';
    strcat(a, b);
    strcat(a, temp);

    printf("%s", a);
}
