#include <stdio.h>
int main(){
    int n,i,key,j;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    scanf("%d",&key);
    for(i=0;i<n;i++){
        if(a[i]==key){
            for(j=i;j<n-1;j++)
                a[j]=a[j+1];
            n--; i--;
        }
    }
    for(i=0;i<n;i++) printf("%d ",a[i]);
}
