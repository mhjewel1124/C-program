#include <stdio.h>

int main(){
    char a[100], b[100];
    gets(a);
    gets(b);

    int i=0;

    while(a[i] && b[i] && a[i]==b[i])
        i++;

    if(a[i] < b[i]) printf("a is smaller");
    else if(a[i] > b[i]) printf("b is smaller");
    else printf("Equal");
}
