#include <stdio.h>
int main() {
    int n, rev=0, temp, r;
    printf("Enter number: ");
    scanf("%d",&n);
    temp=n;
    while(temp!=0){
        r=temp%10;
        rev=rev*10+r;
        temp/=10;
    }
    if(rev==n) printf("Palindrome");
    else printf("Not Palindrome");
    return 0;
}
