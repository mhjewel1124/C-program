#include <stdio.h>
int main() {
    int i,n,temp,r,sum;
    for(i=1;i<=1000;i++){
        n=i; sum=0; temp=n;
        while(temp!=0){
            r=temp%10; sum+=r*r*r; temp/=10;
        }
        if(sum==n) printf("%d ",n);
    }
    return 0;
}
