#include <stdio.h>
int main(){
    int n,i,j=0;
    scanf("%d",&n);
    int arr[n], temp[n];
    for(i=0;i<n;i++) scanf("%d",&arr[i]);
    for(i=0;i<n-1;i++)
        if(arr[i]!=arr[i+1]) temp[j++]=arr[i];
    temp[j++]=arr[n-1];
    for(i=0;i<j;i++) printf("%d ",temp[i]);
}
