#include <stdio.h>

int main(){
    char s[200];
    gets(s);

    if(s[0] != ' ')
        printf("%c ", s[0]);

    for(int i=0;s[i];i++)
        if(s[i]==' ' && s[i+1]!=' ')
            printf("%c ", s[i+1]);
}
