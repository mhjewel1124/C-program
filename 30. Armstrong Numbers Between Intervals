#include <stdio.h>
int main() {
    int low, high, i, temp, r, sum;
    printf("Enter range: ");
    scanf("%d%d",&low,&high);
    for(i=low;i<=high;i++){
        temp=i; sum=0;
        while(temp!=0){
            r=temp%10; sum+=r*r*r; temp/=10;
        }
        if(sum==i) printf("%d ",i);
    }
    return 0;
}
