#include <stdio.h>
struct D{ int ft, in; };
int main(){
int n; scanf("%d", &n);
struct D d, sum={0,0};
for(int i=0;i<n;i++){
scanf("%d %d", &d.ft, &d.in);
sum.ft += d.ft;
sum.in += d.in;
}
sum.ft += sum.in/12;
sum.in %= 12;
printf("Sum = %d ft %d in", sum.ft, sum.in);
return 0;
}
