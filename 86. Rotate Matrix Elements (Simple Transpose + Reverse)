#include <stdio.h>
int main(){
    int a[10][10], r, c;
    scanf("%d%d",&r,&c);

    for(int i=0;i<r;i++)
        for(int j=0;j<c;j++)
            scanf("%d",&a[i][j]);

    // transpose
    for(int i=0;i<r;i++){
        for(int j=i;j<c;j++){
            int t=a[i][j];
            a[i][j]=a[j][i];
            a[j][i]=t;
        }
    }

    // reverse each row
    for(int i=0;i<r;i++)
        for(int j=0;j<c/2;j++){
            int t=a[i][j];
            a[i][j]=a[i][c-j-1];
            a[i][c-j-1]=t;
        }

    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++)
            printf("%d ",a[i][j]);
        printf("\n");
    }
}
